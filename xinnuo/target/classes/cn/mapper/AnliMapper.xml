<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.mapper.AnliMapper" >
  <select id="chaAnli"  resultType="Anli">
  SELECT * FROM anli
  </select>
     <select id="chakans" resultType="Anli">
  SELECT * FROM anli where id=#{id}
  </select>
</mapper>
